// && runs only when both conditions are true 
if (condition1 && condition2){}

// || runs only when either one condition must be true 
if (condition1 || condition2){} 

// ! not operator used for opposite outcome 
if (!condition1){}





